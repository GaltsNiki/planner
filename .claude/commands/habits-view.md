You will be implementing a design change to the Habits view in this codebase.

$ARGUMENTS

ALGORITHM:

1. Use the Playwright MCP to take screenshots of the current Habits view.
2. Pass these screenshots to the Claude Code to get advice on how to upgrade this view.
3. Use the advice from Claude Code to upgrade the view.

IMPORTANT:
Only do this for front-end / UI changes to the Habits view.
Do not modify data models, business logic, or other views (Daily, Weekly, Overview) unless the change is a shared component also used by the Habits view.

Once this change is built, make sure to write the changes you made to a file called frontend-changes.md.
Do not ask for permission to modify this file, assume you can always do it.
